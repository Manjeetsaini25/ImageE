import { Button } from "@/components/ui/button";

export default function Home() {
  return (
   <div className="bg-black">
   This is my new Project --- Image Editor
   <Button variant = "primary">Submit</Button>
   <Button variant = "glass">Submit</Button>
    </div>
  );
}
